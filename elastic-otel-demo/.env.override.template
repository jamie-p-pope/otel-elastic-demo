# ============================================================
# Elastic OTel Demo — .env.override template
#
# Copy this file to .env.override inside your cloned
# elastic/opentelemetry-demo directory and fill in your values.
#
#   cp .env.override.template opentelemetry-demo/.env.override
#
# The Elastic demo sends telemetry directly to Elasticsearch
# (not via the OTLP ingest endpoint). You need:
#   - Your Elasticsearch endpoint URL
#   - An API key (Kibana → Stack Management → API Keys)
# ============================================================

# ------------------------------------------------------------
# REQUIRED: Elasticsearch endpoint + API key
#   Endpoint format: https://<deployment-id>.es.<region>.aws.elastic.cloud:443
#   Get API key: Kibana → Stack Management → API Keys → Create API key
# ------------------------------------------------------------
ELASTICSEARCH_ENDPOINT=https://<your-deployment>.es.us-east-1.aws.elastic.cloud:443
ELASTICSEARCH_API_KEY=<your-api-key>

# ------------------------------------------------------------
# PORT OVERRIDES (optional)
# Uncomment if running alongside the vanilla OTel demo on
# the same host to avoid port 8080/8089 conflicts.
# ------------------------------------------------------------
#ENVOY_PORT=8180
#LOCUST_WEB_PORT=8189

# ------------------------------------------------------------
# LOCUST SETTINGS (optional)
# ------------------------------------------------------------
#LOCUST_USERS=10
#LOCUST_SPAWN_RATE=1
#LOCUST_AUTOSTART=true

# ------------------------------------------------------------
# ENVIRONMENT LABEL (optional)
# Adds a deployment.environment attribute to all telemetry.
# ------------------------------------------------------------
#DEPLOYMENT_ENVIRONMENT=demo
